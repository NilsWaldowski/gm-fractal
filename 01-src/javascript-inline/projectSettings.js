window.project = {
    config: {
        system: {
            code: 'dev', // dev/stage/live (CMS application context)
            baseUrl: 'http://bc-dev.im-kundendialog.de' // CMS baseUrl
        }
    },
    // if necessary
    locallang: {
        more: 'more'
    }
}
